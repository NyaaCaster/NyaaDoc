# NyaaDoc V1 阶段交接 001

> **阶段**：P0 立项（方案确定与文档建立）
> **日期**：2026-09-18
> **下一阶段**：P1 工程骨架与上游基线

---

## 交接目的

本文件用于在新对话中快速接续 NyaaDoc V1 开发。继续工作前请依次阅读：

1. `.docs/开发计划-SSOT.md` — **唯一事实来源**，含架构、约束、进度、验证口径
2. `.docs/设计审核.md` — 关键决策点（D1–D10）、风险清单（R1–R8）、待拍板事项
3. `.docs/初始设计.md` — 需求来源与验收口径（F1–F8）
4. `CLAUDE.md` — 项目规则与硬约束

---

## 当前进度

**P0 立项阶段已完成，尚无代码产物。** 本轮全部工作为**调研、选型、可行性实测与文档建立**。

已完成：

- ✅ 排除 Obsidian 生态路线（Quartz / Digital Garden / Perlite / Webpage HTML Export 全部只读，不满足"Web 端增删改查"）
- ✅ 完成 8 个自托管知识库横向评测，选定 **Docmost**（21.6k⭐、AGPL-3.0、页面级发布开关、本地账号登录）
- ✅ 实测 Docmost 功能：单篇 `.md` 导出、整 Space 层级化 Markdown ZIP 导出、免鉴权公开页、编辑需登录
- ✅ **实测品牌化可行性**：`i18next-http-backend` 运行时加载翻译 → 覆盖静态文件即可换文案与图标，**无需编译前端**
- ✅ 实测 `zh-CN` 语言包已存在且完整（`https://demo.docmost.com/locales/zh-CN/translation.json` 返回 200 完整中文）
- ✅ 核实上游版本：最新 release **v0.96.0**，镜像 `0.96.0` = `latest`，digest `sha256:b56947fc…`，209.3 MB
- ✅ 核实 macmini 环境：`3300`/`3301`/`43083` 空闲、磁盘 151 GB 可用、内存 11 GB 可用、Docker 29.6.1
- ✅ 澄清 `https.conf` 中 `43083` 仅为注释文字（CLIProxyAPI 实际监听 `48317`），端口确可用
- ✅ 建立四份文档：`.docs/初始设计.md`、`.docs/设计审核.md`、`.docs/开发计划-SSOT.md`、`.docs/阶段交接-001.md`
- ✅ 建立项目级 `CLAUDE.md`（项目规则与八条硬约束）
- ✅ **清空远端 `NyaaCaster/NyaaDoc` 的废弃残留文件**（Docusaurus/Netlify 遗留），仓库现为 0 文件

**本轮未做**：任何应用代码、镜像构建、容器启动、macmini 变更、上游镜像拉取。

**追加完成（2026-09-18，P1 部分开展）**：

- ✅ **清空**远端 `NyaaCaster/NyaaDoc` 的废弃残留文件——`README.md`、`docusaurus.config.js` 系该仓库早期 Docusaurus/Netlify 尝试的遗留，**与本知识库项目无关**（commit `50d5b91`、`8739fa1`）。**后续不得引用或复用这些旧内容。**
- ✅ 建立项目级 `CLAUDE.md`（八条硬约束）。
- ✅ 建立 `.gitignore`（Python/Node/Docker/`.env`/`.ref` 全覆盖）。
- ✅ 建立 `meta.json`（项目元数据单一来源：上游版本/digest、端口、镜像名、代码签名）。
- ✅ 纳入 `LICENSE`（AGPL-3.0 全文 + 上游 Docmost 归属声明 + §5(a) 修改声明）。
  - ⚠️ 该文件初始版本系从其他项目复制而来，已修正两处：① 项目描述错误（原为 "A React-based AI chat web application"）；② 补齐 AGPL §5(a) 要求的"已修改"声明。
- ✅ 本地 `git init`（分支 `main`）→ 关联干净远端 → 逐文件提交 `e93d28f`（8 文件 / 1573 行）。
- ✅ 与远端废弃历史以 `--allow-unrelated-histories` **合规合并**（**未使用** force push / rebase）→ 推送成功 `8739fa1..3d5a205`。
- ✅ **推送前完成 secret 终检**：清理了 `CLAUDE.md` 中硬编码的私有仓库地址、文档中真实的部署域名与内网 IP，统一改为占位符（`<PRIVATE_REGISTRY>` / `<YOUR_DOMAIN>`）。

**P1 剩余**：`docker pull docmost/docmost:0.96.0` 并进容器核实 `/app/apps/client/dist/locales/` 与 `icons/` 是否存在（**需用户许可运行容器**）。

---

## 本轮已实现（按文件）

| 文件 | 内容 |
|---|---|
| `.docs/初始设计.md` | 设计目的、项目定位（上游/品牌/发行三层）、F1–F8 需求与验收口径、技术栈、部署目标、四类硬约束 |
| `.docs/设计审核.md` | 三层架构、**品牌化机制实测证据链**、选型对比、D1–D10 关键决策点、R1–R8 风险、对标分析、4 项待拍板 |
| `.docs/开发计划-SSOT.md` | 上游锁定值、品牌化契约（覆盖路径表）、目录结构、环境变量表、V1(P0–P5)/V2(P6–P7) 划分、验证命令、提交规范 |
| `.docs/阶段交接-001.md` | 本文件 |

---

## 仍需继续验证 / 待处理

### A. 决策点（对应《设计审核》第四节 D1–D10）

| 编号 | 事项 | 建议 | 状态 |
|---|---|---|---|
| D4 | 应用端口 | `127.0.0.1:3300`（已实测空闲） | 🟡 待拍板 |
| D5 | HTTPS 方式 | 复用现有 LE SAN 证书（免自签） | 🟡 待拍板 |
| D7 | 上游版本策略 | 锁定 `0.96.0` + digest，不用 `latest` | 🟡 待拍板 |
| D8 | 中文化产出 | V1 仅本地覆盖，不回馈上游 Crowdin | 🟡 待拍板 |
| D9 | 代码签名落点 | `Dockerfile` LABEL + `branding/version.json` | ⬜ 待确认 |
| D10 | 数据备份 | P3 交付定期 `pg_dump` 脚本 | ⬜ 待确认 |

D1（仓库形态）、D2（二开深度）、D3（品牌化实现）、D6（公开访问范围）**已定**，见《设计审核》第四节。

### B. 执行输入（阻塞 P2/P3/P4，需用户提供或拍板）

| 编号 | 事项 | 说明 |
|---|---|---|
| I1 | **nginx 鉴权方案** | ⚠️ 存在冲突：需求要求"公开分享页免鉴权"（F3），而 macmini 现有 https 层惯例为全站 Basic Auth。推荐**按路径分流**：`/share/*` 等公开路径免 auth，其余加 auth |
| I2 | **SMTP 邮件配置** | 不配则"邀请成员 / 忘记密码"不可用（首次 owner 注册不受影响）。是否配置、用哪个邮箱 |
| I3 | **部署域名（`APP_URL`）** | 文档中为 `<YOUR_DOMAIN>` 占位，部署前必须确定实际域名 |
| I4 | **品牌素材** | Logo / Favicon 图源；未提供则先用占位图（不阻塞开发，影响 P2 交付质量） |
| I5 | **产品名替换口径** | 界面内 "Docmost" 字样是否全量替换为 NyaaDoc，或保留 "Powered by Docmost" 归属声明 |
| I6 | **公网开放方式** | 是否为 nginx `43083` 新增路由器 TCP 端口转发 |
| I7 | **README.md** | 公开仓库尚无 README，是否创建及内容口径 |

### C. 待授权（一次性动作，未授权不执行）

| 编号 | 事项 | 说明 |
|---|---|---|
| A1 | **运行容器核实静态资源路径** | `docker run --rm docmost/docmost:0.96.0 ls …`（只读、跑完即删）；**P1 收尾与 P2 解锁的前提** |
| A2 | **git 历史压平** | 远端仍保留废弃仓库的 5 个旧提交。压平需 `--force`（规范禁止，须明确授权），或删除并重建仓库 |

### 待验证（技术）

1. 运行容器内 `/app/apps/client/dist/locales/` 与 `icons/` 实际路径（**P1，解锁 P2 的前提**）
2. 品牌资产覆盖后的渲染效果与 `docker inspect` 可见的代码签名（P2）
3. nginx `43083` 的 WebSocket 透传（P4）
4. F1–F8 端到端验收（P5）

### 风险提醒

- 🔴 **R1 AGPL 合规**：公开仓库即满足源码义务，但必须保留 LICENSE 与修改声明，EE 目录不得改动
- 🔴 **R2 公网暴露动态应用**：仅回环绑定 + nginx 限流 + 强口令 + 跟进上游安全更新
- 🔴 **R3 敏感内容误公开**：分享 = 互联网可见，**上线前必须产出可公开内容清单**
- 🟡 **R4/R5 品牌化失效**：上游升级或 `dist` 结构变更会导致覆盖失效 → 构建期校验 + 冒烟验证脚本

---

## 续接提示词

继续 NyaaDoc V1 开发时，必须以 **plan 模式**推进。先读 `.docs/开发计划-SSOT.md`（唯一事实来源）与 `.docs/设计审核.md`，不要在提示词里重复已完成事项。

当前处于 **P1 进行中**：工程骨架已完成（远端废弃内容已清空；`CLAUDE.md` / `.gitignore` / `meta.json` / `LICENSE` 已建立；首次提交 `e93d28f` 与文档更新 `36a3d06` 均已推送）。**P1 只剩最后一项**：`docker pull docmost/docmost:0.96.0` 并**进容器核实 `/app/apps/client/dist/locales/` 与 `icons/` 是否存在**——这是解锁 P2 薄镜像的前提，**须先取得用户对运行容器的许可**。完成即可标记 P1 ✅ 并进入 P2。

开工前请先确认本文件「待拍板与待授权汇总」的 A / B / C 三组是否已全部拍板。

关键约束（不得违背）：V1 只做**薄镜像品牌化**（不编译前端），源码级外观改造归 V2 且**禁止预先铺开**；上游按 v0.96.0 + digest 锁定，不用 `latest`；应用端口仅绑定 `127.0.0.1`，公网只经 nginx `43083 ssl`；`APP_SECRET` ≥32 字符且只存 `.env`；自动化脚本一律 Python，禁止 `.ps1`/`.sh`；私有仓库地址禁止硬编码，只从 `.env` 的 `PRIVATE_DOCKER_REGISTRY_HOST` 注入并在输出中掩码；不提交 `.env`/`.env.*`（`.env.example` 除外）/`node_modules`/`dist`/数据库卷；保留上游 LICENSE 且显著标注"基于 Docmost 修改"；EE 目录（`apps/*/src/ee`、`packages/ee`）原样不动；核心文件保留运行时可见的代码签名 `Nyaa be with you.`；Git 逐文件 `git add`，禁 `Co-Authored-By` 与 force push，推送走 `$GITHUB_PAT`。

在执行任何容器启动或 macmini 变更前，先取得用户明确许可。
